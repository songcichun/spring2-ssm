spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ssm
    username: root
    password: ryit

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.ryit.entity
